# Copy this file to ".env" and adjust values for your local setup.
# Vite only exposes vars prefixed with VITE_ to the app (via import.meta.env).

# Base URL of the backend API.
VITE_API_URL=http://localhost:8000/api
